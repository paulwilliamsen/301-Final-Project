<% events.forEach(item => { %>
<section class="event-section">
  <p><%= item.date %></p>
  <p><%= item.start_time %></p>
  <p><%= item.title %></p>
  <p><%= item.description %></p>
</section>
  <% }) %>
